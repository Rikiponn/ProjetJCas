program
	a,b,c,d,e,f:float;
	g,h,i,j:integer;
	k,l,m,n,o:float;
	T : array[1 .. 5] of array[1 .. 10] of array[1..2] of integer ;

begin
	read(a);
	b :=  1+5;
	FOR a := 1 TO 10 DO b:=b; END;
	FOR a := 10 DOWNTO 1 DO b := b-1; END;
	WHILE a = a DO b := b; END;
	NEW_LINE;
	read(a);
    write("OKI");
    IF 1 = 2 THEN a := b; ELSE b := a; END;
    IF a=b THEN a := b; END;
end.
