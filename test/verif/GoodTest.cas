program
    a, b : integer;
    c,d : boolean;
    e : array[1..10] of integer;
    f : Array[1..10] of integer;
    g : array[1..5] of array[1..10] of integer;
    h : array[1..10] of array[1..5] of boolean;
    
    i : array[10..1] of integer;
    j : Array[10..1] of integer;
    k : array[10..1] of array[1..5] of integer;
    l : array[1..10] of array[5..1] of boolean;
    
    m : 1 .. 12;
    n : 1 .. 12;
    o, p: float;

begin
    a := 214748;
    b := -214748;
    c := true;
    d := false;

    o := a;

    e := f;
    g[1] := e;
    --z := 12;

    d := a = b;
    d := a < b;
    d := a > b;
    d := a <= b;
    d := a >= b;
    d := a /= b;

    d := o = b;
    d := o < b;
    d := o > b;
    d := o <= b;
    d := o >= b;
    d := o /= b;

    d := o = p;
    d := o < p;
    d := o > p;
    d := o <= p;
    d := o >= p;
    d := o /= p;

    d := a = p;
    d := a < p;
    d := a > p;
    d := a <= p;
    d := a >= p;
    d := a /= p;


    d := not c;
    d := c and d;
    d := c or d;

    
    a := +a;
    a := -a;
    o := +o;
    o := -o;
    
    a := a + a;
    o := a + o;
    o := o + a;
    o := o + o;

    a := a * a;
    o := a * o;
    o := o * a;
    o := o * o;

    a := a - a;
    o := a - o;
    o := o - a;
    o := o - o;


    b := b div a;
    b := b mod a;

    o := b / a;
    o := b / o;
    o := o / a;
    o := o / p;

end.
